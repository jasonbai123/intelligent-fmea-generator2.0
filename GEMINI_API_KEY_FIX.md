# 🔑 Gemini API Key 已泄露 - 解决方案

## ❌ 当前错误

```
ApiError: 403 Forbidden
Your API key was reported as leaked. Please use another API key.
```

**原因**: 您使用的 Gemini API Key 已被标记为泄露/已泄露，Google 已禁用此密钥。

---

## ✅ 解决步骤

### 第1步：生成新的 API Key

1. **访问 Google AI Studio**
   ```
   https://aistudio.google.com/app/apikey
   ```

2. **登录您的 Google 账户**

3. **创建新的 API Key**
   - 点击 **"Create API Key"** 按钮
   - 可能需要选择一个项目（如果没有项目，创建一个新项目）
   - 复制新生成的 API Key
   - 格式类似：`AIzaSy...`

4. **删除旧的 API Key**（推荐）
   - 在同一个页面中
   - 找到旧的 API Key
   - 点击删除按钮
   - 这样可以防止混淆

---

### 第2步：更新应用设置

1. **清除浏览器缓存**（必须！）
   ```
   按 Ctrl + Shift + Delete
   → 勾选"缓存的图片和文件"
   → 时间范围选"全部时间"
   → 点击"清除数据"
   ```

2. **打开应用**
   ```
   https://intelligent-fmea-generator2.pages.dev
   ```

3. **进入 AI 设置**
   - 点击左侧边栏的 **"设置"**
   - 点击 **"AI API 设置"**

4. **配置 Gemini**
   - **服务商**: 选择 **"Google Gemini"**
   - **模型**: 选择 **"gemini-2.5-pro-preview-03625"** 或 **"gemini-1.5-pro"**
   - **API 密钥**: 输入**新生成的** API Key
   - 点击 **"保存设置"**

---

### 第3步：测试生成

1. **回到 FMEA 生成页面**

2. **输入测试内容**（复制粘贴）

   **DFMEA 测试内容**：
   ```
   电动汽车动力电池系统，包含：
   - 锂离子电池包（400V，60kWh）
   - 电池管理系统（BMS）：SOC监控、均衡管理、热管理
   - 液冷热管理系统：温度控制、循环泵、散热器
   - 高压安全系统：继电器、熔断器、预充电电路
   - 充电接口：AC充电（7kW）、DC快充（60kW）
   ```

3. **点击 "开始 DFMEA 分析"**

4. **等待生成**（10-30秒）

---

### 第4步：验证结果

**成功标志**：
- ✅ 生成成功，无错误
- ✅ 内容为**全中文**
- ✅ **所有字段完整**
- ✅ S/O/D/AP 评分正确
- ✅ 符合 AIAG & VDA 标准
- ✅ 可以连续多次生成

---

## 🎯 为什么 API Key 会泄露？

**常见原因**：
1. API Key 被提交到公开的 GitHub 仓库
2. API Key 在客户端代码中暴露（如浏览器 JavaScript）
3. API Key 被分享给他人
4. API Key 在日志或截图中被泄露

**Google 的保护措施**：
- Google 会监控公开的代码库和泄露的密钥
- 一旦发现泄露，立即禁用该密钥
- 这是保护您账户安全的重要措施

---

## 💡 最佳实践

1. **永远不要**将 API Key 提交到 Git 仓库
2. **永远不要**在前端代码中硬编码 API Key
3. **使用环境变量**存储 API Key
4. **定期轮换** API Key（每3-6个月）
5. **监控使用量**，发现异常立即更换密钥

---

## ⚠️ 关于免费额度

**Gemini API 免费额度**：
- ✅ 每天有免费请求次数
- ✅ 足够个人测试使用
- ✅ 超出后会产生费用

**建议**：
- 测试阶段使用免费额度
- 生产环境使用付费版本（更高稳定性）
- 设置使用量限制和告警

---

## 🔄 如果仍然失败

### 检查清单

- [ ] 已生成**新的** API Key（不是复用旧的）
- [ ] 已清除浏览器缓存
- [ ] 选择了 "Google Gemini"
- [ ] 输入了正确的 API Key（以 AIza 开头）
- [ ] API Key 没有前后空格
- [ ] 选择了支持的模型（gemini-1.5-pro 或 gemini-2.5-pro）

### 常见错误

**错误1：Invalid API Key**
- 检查 API Key 是否正确复制
- 确保没有多余的空格
- 确认是新生成的密钥

**错误2：Quota exceeded**
- 超出免费额度
- 需要等待或升级账户

**错误3：Model not found**
- 检查模型名称拼写
- 使用推荐的模型

---

## 🚀 预期成功

配置正确后：
1. ✅ 第一次生成成功
2. ✅ 第二次生成也成功
3. ✅ 全中文内容
4. ✅ 字段完整
5. ✅ 评分准确
6. ✅ 可以连续多次生成

---

## 📝 重要提示

**关于浏览器缓存**：

为什么必须清除缓存？
- 旧版本的 JavaScript 文件还在浏览器缓存中
- 新版本包含了自动登录和中文提示词的更新
- 不清除缓存会继续使用旧代码

如何确认已使用新版本？
- 按 F12 打开开发者工具
- 切换到 Network 标签
- 刷新页面
- 查找加载的 JS 文件
- 应该看到：`index-D7_bGbWj.js`（新版本）
- 不应该看到：`index-tuk4XzLu.js`（旧版本）

---

**🎯 现在请按照上述步骤操作，然后告诉我结果！**

如果遇到问题，请提供：
1. 浏览器 Console 中的错误信息（按 F12 查看）
2. 错误截图
3. 当前使用的模型名称
